'use strict';

describe('Home Service', function () {
    var service;

    beforeEach((function() {
        inject(function(_homeService_) {
            service = _homeService_;
        });
    }));

    // it('should define a add2Number', function () {
    //     expect(service.add2Number(1,2)).toEqual(3);
    // });
});